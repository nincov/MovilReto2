<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ret1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/iconoprincipal"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/iconoprincipal_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ret1">
        <activity
            android:name=".Sucursales"
            android:exported="false" />
        <activity
            android:name=".Servicios"
            android:exported="false" />
        <activity
            android:name=".Productos"
            android:exported="false" />
        <activity
            android:name=".splash"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Ret1.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>